import { OtherSelectorsDirective } from './other-selectors.directive';

describe('OtherSelectorsDirective', () => {
  it('should create an instance', () => {
    const directive = new OtherSelectorsDirective();
    expect(directive).toBeTruthy();
  });
});
